open Base
open Aoc_lib.Helpers

let day = 10
type t = int 

let parse_input (input: string): t = Int.of_string input

let part1 (input: t): string = Int.to_string input 

let part2 (input: t): string = Int.to_string input
